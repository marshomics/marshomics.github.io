---
title: "EXAMPLE — Tool name"
excerpt: "One-line summary that appears on the Software index page (what the tool does, what problem it solves)."
collection: software
permalink: /software/example-tool
---

Brief description of the tool — what it does, who it's for, what problem it solves.

**Repository:** [github.com/marshomics/example-tool](https://github.com/marshomics/example-tool)

**Paper:** *Author et al., 2025* — link to the publication describing the method.

**Install:**

```bash
pip install example-tool
# or
conda install -c bioconda example-tool
```

**Quick usage:**

```bash
example-tool --input genomes.fasta --output results/
```

Copy this file (rename to a slug like `archeasweep.md`) for each real software release, or delete this placeholder. The `permalink` should match the filename slug.
